---
name: waypoint
description: Use Waypoint MCP when the user asks to read, address, or watch existing browser annotations, including “read my annotations”.
---

Use `read_annotations` for existing requests or `watch_annotations` for a live session and carry actionable work through the tools’ lifecycle; for Variant Intent, submit browser-presentable candidates through `request_variants`, revise an unresolved set with `replace_variants` instead of cancelling, preserve a resolvable Target, leave comparison to Waypoint, and reconcile temporary source after Keep or Cancel.
