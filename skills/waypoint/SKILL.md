---
name: waypoint
description: Use Waypoint MCP when the user asks to read, address, or watch existing browser annotations, including “read my annotations”.
---

Use `read_annotations` for existing requests or `watch_annotations` for a live session, follow the tools’ scope and lifecycle instructions, and carry actionable requests through implementation unless the user explicitly asks only to inspect or monitor.
