# One Annotation owns a Target Set

When feedback applies to multiple places on one page, one Annotation owns the complete Target Set. We rejected linked per-Target Annotations because their separate claims, lifecycle states, Queue rows, and resolutions would expose an internal shortcut as fragmented user and agent workflows; multi-Target Annotations share feedback and lifecycle but do not apply Element edits across their Targets.
